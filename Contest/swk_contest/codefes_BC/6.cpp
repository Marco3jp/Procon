#include <bits/stdc++.h>
#define ll long long
#define all(a) (a).begin(),(a).end()

using namespace std;

int main(int argc, char const *argv[]) {
    string S;
    std::cin >> S;
    std::vector<int> number(26);
    for (size_t i = 0; i < S.size(); i++) {
        
    }
  return 0;
}
