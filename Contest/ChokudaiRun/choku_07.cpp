#include <bits/stdc++.h>
#define ll long long
#define all(a) (a).begin(),(a).end()

using namespace std;

int main(int argc, char const *argv[]) {
  int n;
  scanf("%d",&n);
  string v;
  string temp;
  for (size_t i = 0; i < n; i++) {
    std::cin >> temp;
    v+=temp;
  }
  atoi(v);


  return 0;
}
