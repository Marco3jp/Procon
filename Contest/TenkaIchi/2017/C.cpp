#include <bits/stdc++.h>
#define ll long long
#define all(a) (a).begin(),(a).end()

using namespace std;

int main(int argc, char const *argv[]) {
  ll N;
  scanf("%lld",&N);
  for (size_t h = 0; h < 3500; h++) {
    for (size_t n = 0; n < 3500; n++) {
      //切り上げとそのままで比較してくだしあ
    }
  }
  return 0;
}


/*
w=(H*h*n)/(4*h*n-N*n-N*h)



*/
